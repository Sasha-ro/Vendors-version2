using CatalogService as service from '../../srv/cat-service';
// Duplicate annotations for CatalogService.Vendors removed to avoid conflicts with app/annotations.cds.
// If you want to keep project1-specific annotations, move unique ones here and remove from app/annotations.cds.

